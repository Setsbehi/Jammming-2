// Above the empty object, declare an empty variable that will hold the user’s access token. Хоосон объектын дээр хэрэглэгчийн хандалтын тэмдгийг хадгалах хоосон хувьсагчийг зарлана.

let accessToken;

export default function Spotify() {
  getAccessToken;
  return;
}
